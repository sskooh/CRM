@model IEnumerable<CRM.Areas.Admin.Models.JobProperty>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_layotAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Job.StartJobDateStr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Job.StartJobDateStr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobPropertyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobPropertyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobPropertyID })
        </td>
    </tr>
}

</table>
