@model IEnumerable<CRM.Areas.Admin.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_layotAdmin.cshtml";
}



<p>
    @Html.ActionLink("جدید", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Definition.DefTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Property.PropertyName)
        </th>
    
        <th>
            @Html.DisplayNameFor(model => model.NameFmily)
        </th>
     
   
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Definition.DefTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Property.PropertyName)
        </td>
 
        <td>
            @Html.DisplayFor(modelItem => item.NameFmily)
        </td>
      

        <td>
            @Html.ActionLink("ویرایش", "Edit", new { id=item.CustomerID }) 
   
        </td>
    </tr>
}

</table>
